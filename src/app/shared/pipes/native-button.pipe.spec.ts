import { NativeButtonPipe } from './native-button.pipe';

describe('NativeButtonPipe', () => {
  it('create an instance', () => {
    const pipe = new NativeButtonPipe();
    expect(pipe).toBeTruthy();
  });
});
